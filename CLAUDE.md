# buddhist-data-api 作業ルール
このリポジトリは、空海と真言密教の知識を蓄積する「共有知識バンク」です。
新しいCoworkセッションでこのリポジトリの作業を始めるときは、必ず最初に
このファイルを読んでから作業を始めてください。
---
## 基本方針
### 共有知識バンクであること
このリポジトリは、kaimyo-app(戒名・諷誦文・法話生成アプリ)だけのための
データベースではありません。将来、複数の仏教関連アプリで共有して使うことを
想定しています。例えば:
- 戒名・諷誦文・葬儀法話の生成アプリ
- 葬儀以外の日常の法話生成アプリ
- 仏教関連のブログ制作アプリ
- 真言宗教義の勉強会・学習用アプリ
- その他、これから作るかもしれない仏教関連アプリ
これらのアプリ構想はまだ初期段階ですが、データベース自体は今から
複数用途に対応できる形で蓄積していきます。
### データ蓄積の方針
- 特定のアプリ用に最適化したデータ抽出はしないこと
- 「これは戒名作成には使えないから入れなくていい」のような判断をしないこと
- できるだけ豊かに、できるだけ網羅的にデータを保存すること
- 葬儀には使えなくても、日常の法話やブログ、教義学習で価値がある内容も
  すべて取り込むこと
- 空海の言葉と真言密教の知識を、可能な限り完全な形で蓄積すること
---
## データ抽出と校注作業のやり方
### 最重要原則:PDFが絶対の基準
ユーザー(ケンシン)が渡すPDFは、信頼できる正確な典拠です。
Coworkの仕事は、このPDFを忠実に読み取ってJSONに反映することです。
優先順位は絶対に: PDF > Coworkの知識
PDFと異なる内容をCoworkが勝手に書いてはいけません。
Coworkの知識は、PDFが読めない箇所を埋めるための「補助」としてのみ使います。
### 作業の流れ
1. PDFを直接読み取る
   - これが第一の作業。PDFの内容が絶対の基準。
   - OCRは使いません。SATも基本的には使いません。
   - CoworkがPDFを視覚的に直接読み取って作業します。
2. 読み取れた部分は、PDFの通りにそのまま採用する
   - Coworkの知識で「もっと良い表現があるはず」などと
     勝手に書き換えてはいけない。
   - PDFに書いてあることが、そのまま正しい内容。
3. 読み取れない部分のみ、Coworkの知識で補完する
   - かすれ、汚れ、印刷のにじみなどでPDFから文字が判別できないとき、
     初めてCowork自身が持っている空海の著作の知識を使う。
   - その知識を使って「筋が通るように」補完する。
   - これはあくまで補助的な作業であり、判別できる部分を
     上書きしてはならない。
4. 判断に迷ったときは、必ずユーザーに確認する
   - 「PDFのここがかすれていて読めない、Coworkの知識ではこう推測するが
     確認してほしい」と報告する。
   - 勝手に判断して進めないこと。
5. 修正済みのテキストを data/kukai/ の該当するJSONファイルに追加する。
6. コミットする。
7. push.bat を手動実行してGitHubに反映する(ユーザーが実行)。
---
## フォルダ構成
- data/kukai/ … 空海13著作のJSONデータ(製品データ)
- data/mikkyou/ … 密教辞典系データ(製品データ)
- data/templates/ … 諷誦文・表白テンプレート(製品データ)
- _dev_references/ … 開発作業のための参照資料(製品データではない、
  アプリは参照しない)
重要な区別:
- data/ の中身 = 製品データ(アプリが参照する)
- _dev_references/ の中身 = 開発参照資料(アプリは参照しない)
製品アプリのコードは、data/ の中だけを参照します。
---
## 著作の元PDFファイルの場所
空海の著作のPDFファイルは、すべて以下の場所に保存されています:
C:\Users\user\OneDrive\デスクトップ\DATE\真言\
例:
- 性霊集 → C:\Users\user\OneDrive\デスクトップ\DATE\真言\性霊集\
  - 性霊集 上巻.pdf
  - 性霊集 中巻.pdf
  - 性霊集 下巻.pdf
他の著作も同じ構造で DATE\真言\ 配下に保存されています。
重要: ~/OneDrive/デスクトップ/ドキュメント/ のような場所には
著作のPDFは存在しません。必ず上記の DATE\真言\ 配下を使ってください。
場所が分からない場合は、推測せず、必ずユーザーに確認してください。
---
## 守るべき原則
1. 既存のJSONファイルの構造を勝手に変更しないこと
   - 必ず最新のコミット内容を確認してから、同じ形式で続けること
   - 古いやり方に戻らないこと(作業のやり方は段階的に進化してきている)
2. 典拠を必ず明示すること(空海の精神に従う)
   - どの願文・どの章から引用したかを必ず括弧書きで示す
   - 空海自身が経典の典拠を尊重した態度を、データ作成にも継承する
3. 「賀」と「訶」のような意図的な使い分けは保持すること
   - 真言宗の基本経典では「訶」が通常訳
   - ただし空海『吽字義』では『理趣釈経』を引用するときだけ「賀」を使う
   - これは空海が引用元の表記を尊重した結果であり、機械的に統一しない
4. AIが勝手に作り出した言葉は使わないこと
   - 必ずPDFの内容と、Coworkが持っている空海の著作の正確な知識に基づくこと
5. エラーが出たら勝手にリトライせず、必ず報告すること
6. 一度に大量に処理せず、章ごと・段階的に進めること
   - コンテキスト溢れを防ぐため
7. data/ の中身と _dev_references/ の中身を混同しないこと
---
## 新しいセッションを始めるときの手順
新しいCoworkセッションでこのリポジトリの作業を始めるときは、
以下の手順を踏んでください:
1. このCLAUDE.mdを最初に読む(自動で読まれる)
2. 該当する著作の最新のコミット内容を確認する
3. 該当するJSONファイルの現在の構造と最新の追加内容を確認する
4. 上記の作業ルールに従って、続きを進める
---
## 進捗状況
空海13著作のうち、以下が取り込み済み(または進行中):
- 吽字義
- 弁顕密二教論
- 秘蔵宝鑰
- 性霊集(中巻 進行中 / 最終取り込みページ: p485)
残り約10著作の取り込みが今後の作業対象です。
---
最終更新:2026-04-11
